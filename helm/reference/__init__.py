"""Pristine, unmodified copies of the upstream HELM-MiCE modules.

These exist purely as a correctness/performance baseline: ``tests/`` checks the
optimized implementations in :mod:`helm.modules` against them numerically, and
``benchmarks/`` times the two side by side. Nothing in the training path imports
from here.

Source: https://github.com/Graph-and-Geometric-Learning/helm (Apache-2.0).
"""
